# Configuracoes PDF CAD Converter
# Copie este arquivo para .env e ajuste os valores

# Pasta para monitorar arquivos PDF (obrigatorio)
# Use caminho absoluto para melhor compatibilidade
WATCH_FOLDER=/path/to/pdf/folder

# Pasta onde os JPEGs serao salvos (obrigatorio)
OUTPUT_FOLDER=/path/to/output/folder

# Qualidade JPEG (0-100, padrao: 90)
JPEG_QUALITY=90

# DPI para conversao (padrao: 300)
# Maior DPI = melhor qualidade, arquivos maiores
DPI=300

# Nivel de log (error, warn, info, debug)
LOG_LEVEL=info

# Pasta de logs (opcional, padrao: ./logs)
LOG_FOLDER=./logs

# Caminho do Poppler (opcional, detectado automaticamente)
# Windows: ./tools/poppler/Library/bin (se instalado pelo script)
# Linux: deixe vazio (usara system PATH)
POPPLER_PATH=

# Intervalo de retry em ms para arquivos em uso (padrao: 5000)
RETRY_INTERVAL=5000

# Numero maximo de retries (padrao: 3)
MAX_RETRIES=3
